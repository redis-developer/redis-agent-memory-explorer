{
  "name": "custom-agent-utils",
  "version": "0.0.0",
  "private": true,
  "description": "Redis Agent Memory Explorer",
  "workspaces": [
    "packages/*",
    "backend",
    "frontend"
  ],
  "scripts": {
    "setup": "npm install && npm run build:packages",
    "build:packages": "npm run build -w packages/cau-logger -w packages/cau-redis -w packages/cau-api-server -w packages/agent-memory-ts-sdk -w packages/cau-ram -w packages/context-surfaces-ts-sdk -w packages/cau-context-surfaces",
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "clean": "npm run clean --workspaces",
    "dev": "npm run build:packages && concurrently -n langgraph,api,frontend -c blue,green,magenta \"npm run dev:langgraph\" \"npm run dev:api\" \"npm run dev:frontend\"",
    "dev:frontend": "npm run dev -w frontend",
    "dev:api": "npm run dev -w backend",
    "dev:langgraph": "npm run dev:langgraph -w backend",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
